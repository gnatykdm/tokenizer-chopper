#ifndef DIJKSTRA_H
#define DIJKSTRA_H

#include <vector>
#include <string>

std::vector<int> tokenizeDijkstra(std::vector<int>& tokens);
std::string detokenizeDisjkstra(const std::vector<int> tokens);

#endif // DIJKSTRA_H